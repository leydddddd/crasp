import React from "react";
import ReactDOM from "react-dom/client";
import { ArchiverDashboard } from "@/components/ArchiverDashboard";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ArchiverDashboard />
  </React.StrictMode>
);
